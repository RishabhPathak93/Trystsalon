{
  "version": 2,
  "buildCommand": "apt-get update && apt-get install -y git-lfs && git lfs install && git lfs pull && bun run build",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
